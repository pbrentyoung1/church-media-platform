# AI Prompt Library

## Migration
"Create a Laravel migration for playlists with uuid, tenant_id, title, slug, sort_order."

## API
"Scaffold /api/v1/playlists/{id} returning tenant-scoped videos."

## Device Link
"Generate middleware EnsureRecentTwoFactor requiring fresh TOTP before /link."
